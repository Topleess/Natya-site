services:
  web:
    build: .
    container_name: natya-site-web # Фиксируем имя
    restart: always
    ports:
      - "8081:80"
    networks:
      - main_network

networks:
  main_network:
    external: true
